apiVersion: v1
kind: Service
metadata:
  name: postgres-staging
  namespace: staging
  labels:
    app: postgres-staging
    environment: staging
spec:
  type: ClusterIP
  selector:
    app: postgres-staging
  ports:
  - name: postgres
    port: 5432
    targetPort: 5432
    protocol: TCP
