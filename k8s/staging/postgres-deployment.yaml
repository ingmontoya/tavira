apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-staging
  namespace: staging
  labels:
    app: postgres-staging
    environment: staging
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres-staging
  strategy:
    type: Recreate  # Important for databases
  template:
    metadata:
      labels:
        app: postgres-staging
        environment: staging
    spec:
      containers:
      - name: postgres
        image: postgres:16-alpine
        imagePullPolicy: IfNotPresent

        ports:
        - containerPort: 5432
          name: postgres
          protocol: TCP

        env:
        - name: POSTGRES_DB
          valueFrom:
            configMapKeyRef:
              name: postgres-config-staging
              key: POSTGRES_DB
        - name: POSTGRES_USER
          valueFrom:
            configMapKeyRef:
              name: postgres-config-staging
              key: POSTGRES_USER
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: laravel-env-staging
              key: DB_PASSWORD
        - name: PGDATA
          value: /var/lib/postgresql/data/pgdata

        # Optimized resources for staging (right-sized based on actual usage)
        resources:
          requests:
            cpu: 100m       # Based on 100m actual usage (stable)
            memory: 256Mi   # Based on 200Mi actual usage + 28% headroom
          limits:
            cpu: 400m       # 4x requests for query bursts
            memory: 512Mi   # 2x requests for query result caching

        livenessProbe:
          exec:
            command:
            - pg_isready
            - -U
            - tavira_user_staging
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3

        readinessProbe:
          exec:
            command:
            - pg_isready
            - -U
            - tavira_user_staging
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3

        volumeMounts:
        - name: postgres-storage
          mountPath: /var/lib/postgresql/data

      volumes:
      - name: postgres-storage
        persistentVolumeClaim:
          claimName: postgres-pvc-staging
