name: Deploy to Staging

on:
  push:
    branches:
      - develop
  workflow_dispatch:

env:
  DOCKER_IMAGE_PHP: ingmontoyav/tavira-app
  DOCKER_IMAGE_NUXT: ingmontoyav/tavira-nuxt
  DEPLOYMENT_NAME: tavira-app-staging
  QUEUE_WORKER_DEPLOYMENT: tavira-queue-worker-staging
  K8S_NAMESPACE: default

jobs:
  deploy-staging:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      # 1️⃣ Checkout
      - name: Checkout code
        uses: actions/checkout@v4

      # 2️⃣ Set up Docker Buildx
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # 3️⃣ Login Docker Hub
      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      # 4️⃣ Extract version for staging
      - name: Extract version from commit
        id: version
        run: |
          SHORT_SHA=$(echo ${{ github.sha }} | cut -c1-7)
          echo "tag=staging-v$(date +%Y%m%d)-${SHORT_SHA}" >> $GITHUB_OUTPUT
          echo "short_sha=${SHORT_SHA}" >> $GITHUB_OUTPUT

      # 5️⃣ Build & push PHP/FPM image for staging
      - name: Build & Push PHP Docker image (staging)
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./docker/Dockerfile.php
          push: true
          tags: |
            ${{ env.DOCKER_IMAGE_PHP }}:staging
            ${{ env.DOCKER_IMAGE_PHP }}:${{ steps.version.outputs.tag }}
          cache-from: type=registry,ref=${{ env.DOCKER_IMAGE_PHP }}:staging
          cache-to: type=inline
          build-args: |
            BUILD_ENV=staging

      # 6️⃣ Build & push Nuxt image for staging
      - name: Build & Push Nuxt Docker image (staging)
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: |
            ${{ env.DOCKER_IMAGE_NUXT }}:staging
            ${{ env.DOCKER_IMAGE_NUXT }}:${{ steps.version.outputs.tag }}
          cache-from: type=registry,ref=${{ env.DOCKER_IMAGE_NUXT }}:staging
          cache-to: type=inline
          build-args: |
            BUILD_ENV=staging

      # 7️⃣ Configure kubectl
      - name: Set up kubectl
        uses: azure/k8s-set-context@v4
        with:
          method: kubeconfig
          kubeconfig: ${{ secrets.KUBE_CONFIG }}

      # 8️⃣ Verify cluster connection
      - name: Verify cluster connection
        run: |
          echo "🔍 Checking cluster connection..."
          kubectl cluster-info
          kubectl get nodes
          echo "✅ Connected to cluster"

      # 9️⃣ Update main app deployment
      - name: Update Kubernetes deployment (main app)
        run: |
          echo "🔄 Updating staging app deployment to ${{ steps.version.outputs.tag }}..."
          kubectl set image deployment/${{ env.DEPLOYMENT_NAME }} \
            php-fpm=${{ env.DOCKER_IMAGE_PHP }}:${{ steps.version.outputs.tag }} \
            -n ${{ env.K8S_NAMESPACE }} \
            --record
          echo "✅ Main app deployment updated"

      # 🔟 Update queue worker deployment
      - name: Update Kubernetes deployment (queue worker)
        run: |
          echo "🔄 Updating staging queue worker deployment to ${{ steps.version.outputs.tag }}..."
          kubectl set image deployment/${{ env.QUEUE_WORKER_DEPLOYMENT }} \
            queue-worker=${{ env.DOCKER_IMAGE_PHP }}:${{ steps.version.outputs.tag }} \
            -n ${{ env.K8S_NAMESPACE }} \
            --record
          echo "✅ Queue worker deployment updated"

      # 1️⃣1️⃣ Wait for main app rollout
      - name: Wait for main app rollout to complete
        run: |
          echo "⏳ Waiting for main app rollout to complete..."
          kubectl rollout status deployment/${{ env.DEPLOYMENT_NAME }} \
            -n ${{ env.K8S_NAMESPACE }} \
            --timeout=10m
          echo "✅ Main app rollout completed successfully"

      # 1️⃣2️⃣ Wait for queue worker rollout
      - name: Wait for queue worker rollout to complete
        run: |
          echo "⏳ Waiting for queue worker rollout to complete..."
          kubectl rollout status deployment/${{ env.QUEUE_WORKER_DEPLOYMENT }} \
            -n ${{ env.K8S_NAMESPACE }} \
            --timeout=10m
          echo "✅ Queue worker rollout completed successfully"

      # 1️⃣3️⃣ Run post-deployment tasks
      - name: Run post-deployment tasks
        run: |
          echo "🛠️ Running post-deployment tasks for staging..."

          # Wait a bit for pods to stabilize after rollout
          sleep 5

          # Get running pod name (using correct label)
          POD=$(kubectl get pods -n ${{ env.K8S_NAMESPACE }} \
            -l app=tavira-staging \
            --field-selector=status.phase=Running \
            -o jsonpath='{.items[0].metadata.name}')

          echo "📦 Pod: $POD"

          if [ -z "$POD" ]; then
            echo "❌ No running pod found with label app=tavira-staging"
            echo "Available pods:"
            kubectl get pods -n ${{ env.K8S_NAMESPACE }} -l app=tavira-staging
            exit 1
          fi

          # Clear and cache config
          echo "  → Clearing config cache..."
          kubectl exec -n ${{ env.K8S_NAMESPACE }} $POD -c php-fpm -- php artisan config:clear

          echo "  → Caching config..."
          kubectl exec -n ${{ env.K8S_NAMESPACE }} $POD -c php-fpm -- php artisan config:cache

          # Note: route:cache is skipped for multitenancy apps to avoid route conflicts
          # Routes are dynamically loaded per tenant

          echo "  → Caching views..."
          kubectl exec -n ${{ env.K8S_NAMESPACE }} $POD -c php-fpm -- php artisan view:cache

          # Run migrations
          echo "  → Running central migrations..."
          kubectl exec -n ${{ env.K8S_NAMESPACE }} $POD -c php-fpm -- php artisan migrate --force

          echo "  → Running tenant migrations..."
          kubectl exec -n ${{ env.K8S_NAMESPACE }} $POD -c php-fpm -- php artisan tenants:migrate --force

          echo "✅ Post-deployment tasks completed"

      # 1️⃣4️⃣ Show deployment status
      - name: ✅ Deployment complete
        run: |
          echo "============================================="
          echo "✅ Staging deployment completed successfully!"
          echo "============================================="
          echo ""
          echo "📦 Deployed version: ${{ steps.version.outputs.tag }}"
          echo "🌿 Branch: develop"
          echo "🔖 Commit: ${{ steps.version.outputs.short_sha }}"
          echo ""
          echo "📊 Current deployment status:"
          kubectl get deployment ${{ env.DEPLOYMENT_NAME }} -n ${{ env.K8S_NAMESPACE }}
          kubectl get deployment ${{ env.QUEUE_WORKER_DEPLOYMENT }} -n ${{ env.K8S_NAMESPACE }}
          echo ""
          echo "📋 Running pods:"
          kubectl get pods -n ${{ env.K8S_NAMESPACE }} -l app=tavira-staging
          kubectl get pods -n ${{ env.K8S_NAMESPACE }} -l app=tavira-queue-worker-staging
          echo ""
          echo "🌐 Staging environment available at:"
          echo "   https://staging.tavira.com.co"
          echo "============================================="

      # 1️⃣5️⃣ Notify on failure
      - name: Notify on failure
        if: failure()
        run: |
          echo "❌ Staging deployment failed!"
          echo "Check the logs above for details."
          kubectl get pods -n ${{ env.K8S_NAMESPACE }} -l app=tavira-staging
          kubectl describe deployment ${{ env.DEPLOYMENT_NAME }} -n ${{ env.K8S_NAMESPACE }}
