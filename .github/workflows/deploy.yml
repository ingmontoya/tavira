name: Deploy to Production

on:
  push:
    branches:
      - main
  workflow_dispatch:

env:
  DOCKER_IMAGE_PHP: ingmontoyav/tavira-app
  DOCKER_IMAGE_NUXT: ingmontoyav/tavira-nuxt
  DEPLOYMENT_NAME: tavira-app
  K8S_NAMESPACE: default

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      # 1️⃣ Checkout
      - name: Checkout code
        uses: actions/checkout@v4

      # 2️⃣ Set up Docker Buildx
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # 3️⃣ Login Docker Hub
      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      # 4️⃣ Extract version
      - name: Extract version from commit
        id: version
        run: |
          SHORT_SHA=$(echo ${{ github.sha }} | cut -c1-7)
          echo "tag=v$(date +%Y%m%d)-${SHORT_SHA}" >> $GITHUB_OUTPUT
          echo "short_sha=${SHORT_SHA}" >> $GITHUB_OUTPUT

      # 5️⃣ Build & push PHP/FPM image
      - name: Build & Push PHP Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./docker/Dockerfile.php
          push: true
          tags: |
            ${{ env.DOCKER_IMAGE_PHP }}:latest
            ${{ env.DOCKER_IMAGE_PHP }}:${{ steps.version.outputs.tag }}
          cache-from: type=registry,ref=${{ env.DOCKER_IMAGE_PHP }}:latest
          cache-to: type=inline

      # 6️⃣ Build & push Nuxt image
      - name: Build & Push Nuxt Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: |
            ${{ env.DOCKER_IMAGE_NUXT }}:latest
            ${{ env.DOCKER_IMAGE_NUXT }}:${{ steps.version.outputs.tag }}
          cache-from: type=registry,ref=${{ env.DOCKER_IMAGE_NUXT }}:latest
          cache-to: type=inline

      # 7️⃣ Configure kubectl
      - name: Set up kubectl
        uses: azure/k8s-set-context@v4
        with:
          method: kubeconfig
          kubeconfig: ${{ secrets.KUBE_CONFIG }}

      # 8️⃣ Verify cluster connection
      - name: Verify cluster connection
        run: |
          echo "🔍 Checking cluster connection..."
          kubectl cluster-info
          kubectl get nodes
          echo "✅ Connected to cluster"

      # 9️⃣ Update deployment
      - name: Update Kubernetes deployment
        run: |
          echo "🔄 Updating deployment to ${{ steps.version.outputs.tag }}..."
          kubectl set image deployment/${{ env.DEPLOYMENT_NAME }} \
            setup=${{ env.DOCKER_IMAGE_PHP }}:${{ steps.version.outputs.tag }} \
            php-fpm=${{ env.DOCKER_IMAGE_PHP }}:${{ steps.version.outputs.tag }} \
            -n ${{ env.K8S_NAMESPACE }}
          echo "✅ Deployment updated (init container + php-fpm)"

      # 🔟 Wait for rollout
      - name: Wait for rollout to complete
        run: |
          echo "⏳ Waiting for rollout to complete..."
          kubectl rollout status deployment/${{ env.DEPLOYMENT_NAME }} \
            -n ${{ env.K8S_NAMESPACE }} \
            --timeout=10m
          echo "✅ Rollout completed successfully"

      # 1️⃣1️⃣ Run post-deployment tasks
      - name: Run post-deployment tasks
        run: |
          echo "🛠️ Running post-deployment tasks..."

          # Wait a bit for pods to stabilize after rollout
          sleep 5

          # Get running pod name (using correct label 'app=tavira' and phase=Running)
          POD=$(kubectl get pods -n ${{ env.K8S_NAMESPACE }} \
            -l app=tavira \
            --field-selector=status.phase=Running \
            -o jsonpath='{.items[0].metadata.name}')

          echo "📦 Pod: $POD"

          if [ -z "$POD" ]; then
            echo "❌ No running pod found with label app=tavira"
            echo "Available pods:"
            kubectl get pods -n ${{ env.K8S_NAMESPACE }} -l app=tavira
            exit 1
          fi

          # Clear and cache config
          echo "  → Clearing config cache..."
          kubectl exec -n ${{ env.K8S_NAMESPACE }} $POD -c php-fpm -- php artisan config:clear

          echo "  → Caching config..."
          kubectl exec -n ${{ env.K8S_NAMESPACE }} $POD -c php-fpm -- php artisan config:cache

          # Note: route:cache is skipped for multitenancy apps to avoid route conflicts
          # Routes are dynamically loaded per tenant

          echo "  → Caching views..."
          kubectl exec -n ${{ env.K8S_NAMESPACE }} $POD -c php-fpm -- php artisan view:cache

          # Run migrations
          echo "  → Running central migrations..."
          kubectl exec -n ${{ env.K8S_NAMESPACE }} $POD -c php-fpm -- php artisan migrate --force

          echo "  → Running tenant migrations..."
          kubectl exec -n ${{ env.K8S_NAMESPACE }} $POD -c php-fpm -- php artisan tenants:migrate --force

          echo "✅ Post-deployment tasks completed"

      # 1️⃣2️⃣ Show deployment status
      - name: ✅ Deployment complete
        run: |
          echo "============================================="
          echo "✅ Deployment completed successfully!"
          echo "============================================="
          echo ""
          echo "📦 Deployed version: ${{ steps.version.outputs.tag }}"
          echo ""
          echo "📊 Current deployment status:"
          kubectl get deployment ${{ env.DEPLOYMENT_NAME }} -n ${{ env.K8S_NAMESPACE }}
          echo ""
          echo "📋 Running pods:"
          kubectl get pods -n ${{ env.K8S_NAMESPACE }} -l app=tavira
          echo ""
          echo "🌐 Application available at: https://tavira.com.co"
          echo "============================================="
